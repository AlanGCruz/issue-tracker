PORT=3030
# choose: pg | mysql | sqlite (and later: mongo for a separate adapter)
DB_VENDOR=pg

# for Postgres (Docker from earlier)
DATABASE_URL=postgres://postgres:postgres@localhost:5433/issuetracker
# if you switch to MySQL instead:
# DATABASE_URL=mysql://user:password@localhost:3306/issuetracker
# optional (sqlite local file if you want ultra quick local dev):
# SQLITE_FILE=./data.sqlite
DB_NAME=issuetracker
DB_USER=postgres
DB_PASS=postgres
DB_HOST=localhost
DB_PORT=5433